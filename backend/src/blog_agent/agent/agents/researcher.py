from typing import Optional

from src.blog_agent.agent.agents.base_agent import BaseAgent
from config.settings import settings


class ResearcherAgent(BaseAgent):
    """研究员 Agent：负责调研、搜索资料、分析同类文章
    采用 ReAct 模式：思考(Thought) → 搜索(Action) → 观察(Observation) → 总结
    """

    name = "researcher"
    role = "调研员"
    system_prompt = """你是一个专业的调研员，采用 ReAct（思考-行动-观察-总结）模式工作，负责为博客写作提供可靠、有时效性的调研资料。

【任务路由规则——必须严格遵守】
1. 调研任务分为三种类型：标题调研、大纲调研、正文资料调研
2. 每次调用只执行一种任务，任务类型由本次指令中的【任务类型】明确指定
3. 只启动被指定的任务：严禁同时执行多个任务，严禁执行未被指定的任务，严禁输出其他任务类型的内容
4. 如果指令中没有明确任务类型，先确认任务类型，不要自行猜测

【通用工作原则】
1. 【思考】先理解本次任务目标，从用户输入中提取关键信息，确定搜索方向，列出搜索关键词清单
2. 【行动】根据关键词清单逐一联网检索相关资料，优先选择近期发布的资料，确保时效性
3. 【观察】对检索到的资料进行质量把关：评估来源可靠性（官方文档/知名媒体/学术论文 vs 个人博客/论坛转载），记录每篇资料的发布时间（精确到年月日），确认来源可靠且时效性符合需求后，再提取关键信息、数据和观点；对存疑或过时的资料标注说明但不作为主要依据
4. 【总结】按照本次任务指定的输出要求，输出结构化的调研结论

你的回答要简洁、有洞察力，包含具体数据和来源，不要泛泛而谈。"""
    model_config_key = "llm_model_researcher"

    def research_for_titles(self, topic: str) -> str:
        """
        标题调研：ReAct 模式分析同类文章标题，避免重复，提供角度建议
        自动开启千问自带联网搜索
        """
        prompt = f"""【任务类型】标题调研（只服务标题创作，不生成标题本身）

【任务目标】
通过联网搜索同类文章，为后续标题创作提供调研依据。

用户主题：{topic}

【执行步骤】

1. 【思考】提取关键信息：
   从用户主题中提取3-5个核心关键词或短语，组成搜索关键词清单。关键词需覆盖主题的核心概念和潜在写作角度，供后续联网搜索使用。

2. 【搜索】明确搜索目的并执行：
   搜索目的是：根据关键词清单检索与该主题相关的同类文章及资料，重点收集这些文章的标题信息。
   请基于联网检索到的真实文章进行分析。

3. 【观察】分析搜索结果，总结三类信息：
   - 标题类型：调研到的标题主要属于哪些类型（数字型"3个技巧"、疑问型"为什么…"、干货型"从零搭建"、对比型"A vs B"、悬念型"你可能不知道"等）
   - 角度侧重：这些标题主要从哪些角度切入（原理讲解、实战教程、踩坑避坑、对比评测、趋势盘点等），哪些角度最热门
   - 重复度：标题在关键词、句式、角度上的重复程度，哪些写法已被大量使用，哪些角度还比较空

4. 【总结】基于观察结果，给出标题创作建议：
   - 推荐的切入角度（2-3个，优先选择重复度低、有差异化的角度）
   - 建议使用的关键词
   - 需要避开的雷区（已被大量使用的标题写法）
   - 独特的差异化方向

控制在400字以内，要具体，不要空话。

【重要约束】
- 联网搜索不超过 3 轮，搜索到足够资料后立即输出总结
- 不要重复输出【思考】【行动】【观察】过程，直接输出最终调研结论
- 如果搜索结果不足，基于已有资料给出结论并标注"资料有限"，不要反复搜索"""

        # 开启千问自带联网搜索，限制输出长度防止无限循环
        return self.chat(prompt, temperature=0.5, enable_search=True, max_tokens=3000)

    def research_for_outline(self, topic: str, selected_title: str, article_style: str = "", article_style_custom: str = "") -> str:
        """
        大纲调研：先分析标题确定搜索方向，再联网调研同类文章的大纲结构
        自动开启千问自带联网搜索
        article_style: 文章风格（popular_science/technical/essay/prose/note/custom）
        article_style_custom: 自定义风格时的用户输入
        """
        # 风格描述
        style_map = {
            "popular_science": "科普类（通俗易懂，面向大众读者）",
            "technical": "技术类（有技术深度，面向有基础的读者）",
            "essay": "论文类（学术严谨，论证严密）",
            "prose": "散文类（文笔优美，抒情叙事）",
            "note": "笔记类（简洁实用，要点清晰）",
        }
        style_desc = style_map.get(article_style, "")
        if article_style == "custom" and article_style_custom:
            style_desc = f"自定义风格：{article_style_custom}"

        style_section = ""
        if style_desc:
            style_section = f"""
文章风格：{style_desc}
（调研时必须优先搜索此风格类型的文章，大纲结构分析也必须符合此风格的特点）
"""

        prompt = f"""【任务类型】大纲调研（只服务大纲创作，不生成大纲本身，不给出内容建议）

【任务目标】
先深度分析用户选择的标题，确定搜索方向，然后联网调研同类文章的大纲结构，为后续大纲创作提供可效仿的结构范本。

【核心输入】
已选标题：{selected_title}
{style_section}
用户主题：{topic}（仅作背景参考，不要让宽泛的主题干扰搜索方向，一切以已选标题和文章风格为核心）

【执行步骤】

1. 【标题深度分析】先仔细分析已选标题，明确：
   - 这篇文章到底在讲什么主题？核心论点或切入点是什么？
   - 目标读者是谁？读者此时期待看到什么内容？
   - 应该搜索什么类型、什么方向的同类文章？
   - 列出3-5个精准的搜索关键词（覆盖标题的核心概念和文章风格）

2. 【联网搜索】根据上述关键词清单，联网检索与该标题和风格相关的同类文章，重点提取这些文章的大纲结构和章节组织方式。请基于联网检索到的真实文章进行分析。

3. 【结构提取】从检索到的文章中提取大纲结构信息：
   - 此类型/主题的文章通常有哪些典型的大纲结构类型？（分点说明，列举2-3种）
   - 每种结构类型的章节顺序是怎样的？各章节的作用是什么？
   - 哪些结构被大量重复使用（雷同度高）？哪些结构比较有新意？

4. 【大纲制定建议】基于结构分析，给出建议：
   - 推荐选择哪种类型的大纲结构？为什么？
   - 这种结构的优势是什么？如何避免与同类文章雷同？
   - （只给出结构层面的建议，不要给出具体内容写什么的建议）

控制在500字以内，要具体，不要空话。

【重要约束】
- 联网搜索不超过 3 轮，搜索到足够资料后立即输出总结
- 不要重复输出【思考】【行动】【观察】过程，直接输出最终调研结论
- 只关注大纲结构层面，不要给出具体内容写什么的建议
- 如果搜索结果不足，基于已有资料给出结论并标注"资料有限"，不要反复搜索"""

        # 开启千问自带联网搜索，限制输出长度防止无限循环
        return self.chat(prompt, temperature=0.5, enable_search=True, max_tokens=3000)

    def research_for_content(self, topic: str, selected_title: str, outline: str, extra_suggestions: str = "", research_mode: str = "full") -> str:
        """
        正文调研：先逐章节分析大纲内容需求，列出调研清单，再严格按清单联网检索
        自动开启千问自带联网搜索
        extra_suggestions: 审稿打回时给出的调研补充建议（需要补充哪些方向、搜什么关键词）
        research_mode: 调研模式
            - "full": 完整调研（首次生成正文时）
            - "supplement": 补充调研（审稿打回后，只针对审稿意见指出的方向补充搜索，保留之前的调研结果）
            - "redo": 重新调研（审稿打回后，完整重新调研，覆盖之前的结果）
        """
        # 调研模式说明
        mode_section = ""
        if research_mode == "supplement":
            mode_section = """
【调研模式：补充调研】
本次是补充调研，不是完整重新调研。
- 只针对审稿意见指出的方向进行补充搜索，不需要重新分析整个大纲
- 不需要重新输出完整的调研清单，只需要输出补充的资料
- 之前的调研结果会保留，本次输出的补充资料会追加到之前的结果后面
- 输出格式：只输出"补充调研资料"部分，不需要重复输出调研清单和资料可靠性说明
"""
        elif research_mode == "redo":
            mode_section = """
【调研模式：重新调研】
本次是完整重新调研，之前的调研结果会被全部覆盖。
- 需要重新分析整个大纲的内容需求，重新列出调研清单
- 需要重新搜索所有需要调研的方向，不能依赖之前的结果
- 输出完整的调研结果（调研清单 + 按章节整理的资料 + 资料可靠性说明）
"""

        suggestion_section = ""
        if extra_suggestions:
            if research_mode == "supplement":
                suggestion_section = f"""
【审稿反馈要求补充调研的方向】
以下是审稿编辑指出的需要补充调研的方向，本次只针对这些内容进行补充检索：
{extra_suggestions}
"""
            else:
                suggestion_section = f"""
【审稿反馈要求补充调研的方向】
以下是审稿编辑指出的需要补充调研的方向，请重点针对这些内容进行检索：
{extra_suggestions}
"""

        # 补充调研模式：简化prompt，只针对审稿意见补充搜索
        if research_mode == "supplement":
            prompt = f"""【任务类型】正文资料补充调研（只补充审稿意见指出的方向，不重新调研全部内容）

【任务目标】
针对审稿意见指出的调研方向，联网检索补充资料，追加到之前的调研结果中。
{mode_section}
{suggestion_section}
【核心原则】
- 只针对审稿意见指出的方向搜索，不要重新搜索其他方向
- 有就是有，没有就是没有，绝对不许编造来源、发布时间或数据
- 如果某个方向搜索不到足够资料，明确标注"该方向资料有限"

【执行步骤】

1. 【分析补充需求】仔细阅读审稿意见，明确需要补充调研的具体方向和关键词。

2. 【针对性搜索】只针对审稿意见指出的方向进行联网检索，每个方向最多检索2-3篇权威资料。

3. 【整理补充资料】对检索到的资料整理，每条标注：
- 【资料内容】：具体内容摘要
- 【来源】：来源名称（有就是有，没有就写"来源未明确"）
- 【发布时间】：YYYY-MM-DD（有就是有，没有就写"时间未标注"）
- 【链接/出处】：URL或出处（有就是有，没有就写"无"）

4. 【输出】只输出补充调研资料，格式：

【补充调研资料】
（按审稿意见的方向分类列出补充资料，每条包含内容/来源/时间/链接）

【补充调研说明】
- 本次补充了哪些方向的资料
- 哪些方向仍然资料不足

【重要约束】
- 联网搜索不超过 2 轮
- 不要输出调研清单（那是完整调研才需要的）
- 绝对不许编造来源、发布时间、链接或数据
- 控制在500字以内，重点突出"""

            return self.chat(prompt, temperature=0.3, enable_search=True, max_tokens=3000)

        # 完整调研 / 重新调研模式：完整prompt
        prompt = f"""【任务类型】正文资料调研（只服务正文写作，不撰写正文本身）
{mode_section}
【任务目标】
先逐章节分析大纲的内容需求，列出调研清单，再严格按清单联网检索真实、可溯源的支撑资料。
{suggestion_section}
【核心原则】
- 数据和案例不是必要的！先分析每个章节到底需要什么：有些章节需要数据支撑，有些需要专业知识解释，有些需要案例，有些纯逻辑论述根本不需要外部资料
- 不需要外部资料的章节就不搜索，不要为了"看起来调研充分"而硬搜无关内容
- 有就是有，没有就是没有，绝对不许编造来源、发布时间或数据

【执行步骤】

1. 【大纲内容需求分析】逐章节分析大纲，判断每个章节的素材需求：
   文章标题：{selected_title}
   文章大纲：
{outline}

   对大纲中的每个章节，逐一判断：
   - 这个章节的核心内容是什么？
   - 需要什么类型的素材？（数据支撑 / 专业知识解释 / 真实案例 / 技术对比 / 最佳实践 / 纯逻辑论述不需要外部资料）
   - 如果需要搜索，搜索关键词是什么？
   - 如果不需要外部资料，明确标注"无需调研"

   输出结构化的调研清单，格式：
   【章节X：章节标题】
   - 素材需求类型：xxx
   - 是否需要搜索：是/否
   - 搜索关键词（如需要）：xxx
   - 不需要搜索的原因（如不需要）：xxx

2. 【严格按清单搜索】根据上述调研清单，只对标注"需要搜索"的章节进行联网检索。
   - 严格使用清单中列出的搜索关键词，不要偏离清单去搜其他内容
   - 每个搜索方向最多检索2-3篇权威资料即可，不要过度搜索
   - 请基于联网检索到的真实资料进行分析

3. 【按清单整理资料】对检索到的资料，按调研清单的章节分类整理：
   - 真实性把关：只采用来源可靠（官方文档/知名媒体/学术论文/权威机构）的资料，剔除无法溯源、个人臆测的内容
   - 时效性标注：每条资料必须标注发布时间（精确到年月日），如果搜索结果中没有明确时间，标注"时间未标注"，绝对不许编造时间
   - 来源标注：每条资料必须标注来源名称和链接，如果搜索结果中没有明确来源，标注"来源未明确"，绝对不许编造来源
   - 按章节归类：每条资料归到对应的调研清单项下

4. 【格式化输出】输出以下三部分内容：

一、调研清单（大纲内容需求分析结果）
按章节列出每个章节的素材需求类型、是否需要搜索、搜索关键词。

二、按章节整理的调研资料
对每个需要搜索的章节，列出检索到的资料，每条格式：
- 【资料内容】：具体内容摘要（数据/知识/案例等）
- 【来源】：来源名称（有就是有，没有就写"来源未明确"）
- 【发布时间】：YYYY-MM-DD（有就是有，没有就写"时间未标注"）
- 【链接/出处】：URL或出处（有就是有，没有就写"无"）

不需要搜索的章节，标注"本章节无需外部调研资料"。

三、资料可靠性说明
- 本次调研共检索了多少个方向、多少篇资料
- 哪些资料来源可靠、时间明确
- 哪些资料来源或时间不明确，使用时需谨慎
- 哪些章节没有找到足够资料，建议写手基于已有知识撰写

【重要约束】
- 联网搜索不超过 3 轮，搜索到足够资料后立即输出
- 不要输出【思考】【行动】【观察】过程，直接输出最终调研结论
- 绝对不许编造来源、发布时间、链接或数据——有就是有，没有就是没有
- 数据和案例不是必要的，不需要的章节就明确说不需要，不要硬凑
- 如果某个方向搜索结果不足，标注"该方向资料有限"，不要反复搜索
- 控制在800字以内，重点突出，不要废话"""

        # 开启千问自带联网搜索，限制输出长度防止无限循环
        return self.chat(prompt, temperature=0.3, enable_search=True, max_tokens=3000)

    def score_titles(self, topic: str, titles: list, research: str) -> list:
        """
        对生成的3个标题进行评分
        评分维度：准确性、独特性、SEO友好度
        返回：[{"title": str, "accuracy": int, "uniqueness": int, "seo": int, "total": float, "pros": [str], "cons": [str]}, ...]
        """
        titles_str = "\n".join([f"{i+1}. {t}" for i, t in enumerate(titles)])

        prompt = f"""请作为专业的标题评审，对以下3个博客标题进行评分。

主题：{topic}

待评分标题：
{titles_str}

调研参考：
{research}

评分维度（每项0-10分）：
1. 准确性：标题是否准确反映文章内容，不夸大、不误导
2. 独特性：标题是否有新意，避免和同类文章雷同
3. SEO友好度：标题是否包含关键词，是否适合搜索引擎收录

请严格按照以下JSON格式输出，不要输出其他内容：
[
  {{
    "title": "标题原文",
    "accuracy": 0-10的整数,
    "uniqueness": 0-10的整数,
    "seo": 0-10的整数,
    "total": 综合分(保留1位小数),
    "pros": ["优点1", "优点2"],
    "cons": ["缺点1", "缺点2"]
  }}
]

注意：
- 综合分 = (准确性 + 独特性 + SEO友好度) / 3
- pros和cons各2条，要具体，不要空话
- 直接输出JSON数组，不要markdown代码块"""

        resp = self.chat(prompt, temperature=0.3)

        # 解析 JSON
        try:
            import json
            # 清理可能的 markdown 代码块
            resp_clean = resp.strip()
            if resp_clean.startswith("```"):
                lines = resp_clean.split("\n")
                if lines[0].startswith("```"):
                    lines = lines[1:]
                if lines and lines[-1].strip() == "```":
                    lines = lines[:-1]
                resp_clean = "\n".join(lines)
            scores = json.loads(resp_clean)
            return scores
        except Exception as e:
            print(f"标题评分解析失败: {e}")
            # 解析失败，返回默认评分
            return [
                {
                    "title": t,
                    "accuracy": 7,
                    "uniqueness": 7,
                    "seo": 7,
                    "total": 7.0,
                    "pros": ["标题结构清晰"],
                    "cons": ["评分解析失败，使用默认分"]
                }
                for t in titles
            ]
