from typing import Optional

from src.blog_agent.agent.agents.base_agent import BaseAgent
from config.settings import settings


class ResearcherAgent(BaseAgent):
    """研究员 Agent：负责调研、搜索资料、分析同类文章
    采用 ReAct 模式：思考(Thought) → 搜索(Action) → 观察(Observation) → 总结
    """

    name = "researcher"
    role = "调研员"
    system_prompt = """你是一个专业的调研员，采用 ReAct（思考-行动-观察-总结）模式工作，负责为博客写作提供可靠、有时效性的调研资料。

【任务路由规则——必须严格遵守】
1. 调研任务分为三种类型：标题调研、大纲调研、正文资料调研
2. 每次调用只执行一种任务，任务类型由本次指令中的【任务类型】明确指定
3. 只启动被指定的任务：严禁同时执行多个任务，严禁执行未被指定的任务，严禁输出其他任务类型的内容
4. 如果指令中没有明确任务类型，先确认任务类型，不要自行猜测

【通用工作原则】
1. 【思考】先理解本次任务目标，从用户输入中提取关键信息，确定搜索方向，列出搜索关键词清单
2. 【行动】根据关键词清单逐一联网检索相关资料，优先选择近期发布的资料，确保时效性
3. 【观察】对检索到的资料进行质量把关：评估来源可靠性（官方文档/知名媒体/学术论文 vs 个人博客/论坛转载），记录每篇资料的发布时间（精确到年月日），确认来源可靠且时效性符合需求后，再提取关键信息、数据和观点；对存疑或过时的资料标注说明但不作为主要依据
4. 【总结】按照本次任务指定的输出要求，输出结构化的调研结论

你的回答要简洁、有洞察力，包含具体数据和来源，不要泛泛而谈。"""
    model_config_key = "llm_model_researcher"

    def research_for_titles(self, topic: str) -> str:
        """
        标题调研：ReAct 模式分析同类文章标题，避免重复，提供角度建议
        自动开启千问自带联网搜索
        """
        prompt = f"""【任务类型】标题调研（只服务标题创作，不生成标题本身）

【任务目标】
通过联网搜索同类文章，为后续标题创作提供调研依据。

【执行步骤】

1. 【思考】提取关键信息：
   从用户主题中提取3-5个核心关键词或短语，组成搜索关键词清单。关键词需覆盖主题的核心概念和潜在写作角度，供后续联网搜索使用。

2. 【搜索】明确搜索目的并执行：
   搜索目的是：根据关键词清单检索与该主题相关的同类文章及资料，重点收集这些文章的标题信息。
   请基于联网检索到的真实文章进行分析。

3. 【观察】分析搜索结果，总结三类信息：
   - 标题类型：调研到的标题主要属于哪些类型（数字型"3个技巧"、疑问型"为什么…"、干货型"从零搭建"、对比型"A vs B"、悬念型"你可能不知道"等）
   - 角度侧重：这些标题主要从哪些角度切入（原理讲解、实战教程、踩坑避坑、对比评测、趋势盘点等），哪些角度最热门
   - 重复度：标题在关键词、句式、角度上的重复程度，哪些写法已被大量使用，哪些角度还比较空

4. 【总结】基于观察结果，给出标题创作建议：
   - 推荐的切入角度（2-3个，优先选择重复度低、有差异化的角度）
   - 建议使用的关键词
   - 需要避开的雷区（已被大量使用的标题写法）
   - 独特的差异化方向

控制在400字以内，要具体，不要空话。"""

        # 开启千问自带联网搜索
        return self.chat(prompt, temperature=0.5, enable_search=True)

    def research_for_outline(self, topic: str, selected_title: str) -> str:
        """
        大纲调研：ReAct 模式分析同类文章大纲结构，避免重复
        自动开启千问自带联网搜索
        """
        prompt = f"""【任务类型】大纲调研（只服务大纲创作，不生成大纲本身）

【任务目标】
基于用户主题和已选标题，联网调研同类文章的大纲结构，为后续大纲创作提供可效仿的结构范本。

【执行步骤】

1. 【思考】分析输入并提取关键信息：
   用户主题：{topic}
   已选标题：{selected_title}
   从两者中提取3-5个核心关键词或短语，组成搜索关键词清单，覆盖主题的核心概念和潜在章节方向。

2. 【搜索】明确搜索目的并执行：
   搜索目的是：检索与该主题和标题相关的同类文章，重点提取这些文章的大纲结构和章节组织方式。
   请基于联网检索到的真实文章进行分析。

3. 【观察】提取并分析检索到的文章大纲结构：
   - 章节构成：同类文章通常包含哪些章节？章节顺序如何组织？
   - 内容重点：哪些内容是读者期待的必含内容？
   - 雷同情况：哪些章节结构被大量重复使用？

4. 【总结】给出可效仿的大纲结构范本：
   - 推荐的章节结构（可直接参考的骨架，说明每章写什么）
   - 必须包含的核心内容
   - 可以增加的差异化内容（避免与同类文章雷同）
   - 需要注意的避坑点

控制在400字以内，要具体，不要空话。"""

        # 开启千问自带联网搜索
        return self.chat(prompt, temperature=0.5, enable_search=True)

    def research_for_content(self, topic: str, selected_title: str, outline: str) -> str:
        """
        正文调研：ReAct 模式根据标题和大纲，联网检索最新的支撑内容和可靠数据
        自动开启千问自带联网搜索
        """
        prompt = f"""【任务类型】正文资料调研（只服务正文写作，不撰写正文本身）

【任务目标】
根据文章大纲，联网检索真实、有时效性的支撑资料，为正文写作提供可引用的素材库。

【执行步骤】

1. 【思考】分析大纲并提取关键信息：
   用户主题：{topic}
   拟定标题：{selected_title}
   文章大纲：
{outline}
   结合大纲各章节内容，确定每个章节需要哪些最新数据、案例和权威资料支撑，列出搜索关键词清单。

2. 【搜索】明确搜索目的并执行：
   搜索目的是：针对大纲中的关键技术点、数据需求，逐一联网检索相关的权威资料。
   请基于联网检索到的真实资料进行分析。

3. 【观察】对检索到的资料做真实性和时效性把关后再提取：
   - 真实性：只采用来源可靠（官方文档/知名媒体/学术论文）的资料，剔除无法溯源、个人臆测的内容
   - 时效性：记录每篇资料的发布时间（精确到年月日），优先采用近期资料，标记或排除过时信息
   - 从通过把关的资料中提取：最新技术版本号、官方数据（具体数字）、权威参考资料、真实案例、Benchmark数据、最佳实践

4. 【总结】输出两部分内容：

一、写作参考资料：
- 关键数据速查（版本号、性能指标等，列出具体数字）
- 可引用的权威来源
- 需要在文章中体现的最新实践
- 注意避免的过时信息

二、资料来源清单：
列出本次联网搜索引用的所有资料，每条包含【来源名称】【发布时间（精确到年月日）】【资料链接或出处】

所有引用的数据和事实必须能在资料来源清单中找到对应出处。
控制在600字以内，数据要具体，来源要明确。"""

        # 开启千问自带联网搜索
        return self.chat(prompt, temperature=0.3, enable_search=True)

    def score_titles(self, topic: str, titles: list, research: str) -> list:
        """
        对生成的3个标题进行评分
        评分维度：准确性、独特性、SEO友好度
        返回：[{"title": str, "accuracy": int, "uniqueness": int, "seo": int, "total": float, "pros": [str], "cons": [str]}, ...]
        """
        titles_str = "\n".join([f"{i+1}. {t}" for i, t in enumerate(titles)])

        prompt = f"""请作为专业的标题评审，对以下3个博客标题进行评分。

主题：{topic}

待评分标题：
{titles_str}

调研参考：
{research}

评分维度（每项0-10分）：
1. 准确性：标题是否准确反映文章内容，不夸大、不误导
2. 独特性：标题是否有新意，避免和同类文章雷同
3. SEO友好度：标题是否包含关键词，是否适合搜索引擎收录

请严格按照以下JSON格式输出，不要输出其他内容：
[
  {{
    "title": "标题原文",
    "accuracy": 0-10的整数,
    "uniqueness": 0-10的整数,
    "seo": 0-10的整数,
    "total": 综合分(保留1位小数),
    "pros": ["优点1", "优点2"],
    "cons": ["缺点1", "缺点2"]
  }}
]

注意：
- 综合分 = (准确性 + 独特性 + SEO友好度) / 3
- pros和cons各2条，要具体，不要空话
- 直接输出JSON数组，不要markdown代码块"""

        resp = self.chat(prompt, temperature=0.3)

        # 解析 JSON
        try:
            import json
            # 清理可能的 markdown 代码块
            resp_clean = resp.strip()
            if resp_clean.startswith("```"):
                lines = resp_clean.split("\n")
                if lines[0].startswith("```"):
                    lines = lines[1:]
                if lines and lines[-1].strip() == "```":
                    lines = lines[:-1]
                resp_clean = "\n".join(lines)
            scores = json.loads(resp_clean)
            return scores
        except Exception as e:
            print(f"标题评分解析失败: {e}")
            # 解析失败，返回默认评分
            return [
                {
                    "title": t,
                    "accuracy": 7,
                    "uniqueness": 7,
                    "seo": 7,
                    "total": 7.0,
                    "pros": ["标题结构清晰"],
                    "cons": ["评分解析失败，使用默认分"]
                }
                for t in titles
            ]
